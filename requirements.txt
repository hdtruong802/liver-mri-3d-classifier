# Môi trường train/tiền xử lý (chạy trên Kaggle). Pin version để tái lập.
# Backend serve tách riêng ở webapp/backend/requirements.txt (AGENTS.md §5).

# --- Data / preprocessing (dùng từ W2) ---
numpy==1.26.4
pandas==2.2.2
PyYAML==6.0.2
SimpleITK==2.4.0
nibabel==5.2.1
scikit-learn==1.5.1
scipy==1.13.1

# --- Deep learning (dùng từ baseline W2 ngày 5) ---
# Pin khớp stack Kaggle GPU; điều chỉnh khi lên Kaggle nếu lệch.
torch==2.3.1
monai==1.3.2

# --- Dev ---
ruff==0.6.9
pytest==8.3.3
