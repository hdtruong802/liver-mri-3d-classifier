"""Lớp dữ liệu: annotation, splits, image index, dataset.

Lưu ý: `dataset.py` import MONAI/torch nên KHÔNG import ở đây — để `annotation`/
`splits`/`build_manifest` chạy được cả khi chưa cài deep-learning stack.
"""
